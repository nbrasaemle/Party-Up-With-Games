<div class="col">
    <div class="card party-cards">
        <div class="card-header">
            {{this.party_name}}
        </div>
        <div class="card-body">
            {{!-- <h5 class="card-title">{{game_name}}</h5> --}}
            <p class="card-text">Date:  {{this.meeting_date}}</p>
            <a href="/parties/{{this.hosted_gameid}}" class="btn btn-primary">View Party</a>
        </div>
    </div>
</div>