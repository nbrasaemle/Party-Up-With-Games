<div class="col">
    <div class="card party-cards">
        <div class="card-header">
            {{games.party_name}}
        </div>
        <div class="card-body">
            {{!-- <h5 class="card-title">{{game_name}}</h5> --}}
            <p class="card-text">Date:  {{games.meeting_date}}</p>
            <a href="/parties/{{games.hosted_gameid}}" class="btn btn-primary">View Party</a>
        </div>
    </div>
</div>